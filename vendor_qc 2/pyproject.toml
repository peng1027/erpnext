[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "vendor_qc"
version = "0.1.0"
authors = [{name="APP Dev"}]
description = "Vendor QC app for Frappe/ERPNext"
readme = "README.md"
requires-python = ">=3.10"
dependencies = ["qrcode[pil]>=7.4"]
